<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guangyou.rareanimal.mapper.ScheduledMapper">

<!--    String selectScheduledById(Long cronId);-->
    <select id="selectScheduledById" resultType="string">
        select cron from t_scheduled where cron_id = #{cronId}
    </select>
</mapper>